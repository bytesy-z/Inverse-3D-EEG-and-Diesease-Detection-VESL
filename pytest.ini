[pytest]
markers =
    unit: Unit tests (fast, isolated, no I/O)
    functional: Functional tests (component integration)
    system: System tests (full pipeline, needs real checkpoints)
    integration: Integration tests (end-to-end, needs backend)
    slow: Slow tests (>5s each)
    regression: Regression tests (bug-triggered)
testpaths = tests
